function search(){
    let input =document.getElementById('pesquisa').value
    input = input.tolowerCase()
    let p = document,getElementByClassName('texto1 texto2')
     
    for(i = 0;< p.length; i++)
        if(!p[])
} 